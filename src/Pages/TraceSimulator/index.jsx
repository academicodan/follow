import { ContainerApp } from '../../Components/ContainerApp'
import style from './styles.module.scss'

export const TraceSimulator = () => {
  return(
    <ContainerApp notStep>
      <div>
        Page TraceSimulator
      </div>
    </ContainerApp>
  )
}